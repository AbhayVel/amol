import { IsAuthorizedDisabledDirective } from './is-authorized-disabled.directive';

describe('IsAuthorizedDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAuthorizedDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
